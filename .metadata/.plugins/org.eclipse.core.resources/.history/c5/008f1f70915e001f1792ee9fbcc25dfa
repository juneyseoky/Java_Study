package numList;

public class Average implements NumberSequence {

	@Override
	public void mtdNumRes(double[] list) {
		double sum = 0;
		double avg = 0;
		for (int i = 0; i < list.length; i++) {
			sum += list[i];
		}
		avg = sum/list.length;
		System.out.printf("가장 작은 값 : %d", smallNum);
	}

}
